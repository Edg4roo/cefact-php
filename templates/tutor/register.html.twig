{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <body class="bg-grey">
    <main class="vh-100 d-flex align-items-center justify-content-center">
        <div class="container d-flex flex-column align-items-center justify-content-center">
            <div class="row">
                <div class="col-12 bg-orange-tutor text-center">
                    Logo
                </div>
            </div>
            <div class="row bg-grey shadow py-5">
                <h1 class="text-center py-3">Regístrate</h1>

                {{ form_start(registrationForm) }}
                {{ form_row(registrationForm.name) }}
                {{ form_row(registrationForm.email) }}
                {{ form_row(registrationForm.phone) }}
                {{ form_row(registrationForm.plainPassword, {
                    label: 'Password'
                }) }}
                <div class="text-center"> <button type="submit" class="btn btn-darkorange text-white w-50 mt-5">Register</button> </div>

                {{ form_end(registrationForm) }}
            </div>
        </div>
    </main>
    </body>
{% endblock %}


